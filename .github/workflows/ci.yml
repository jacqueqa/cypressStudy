name: E2E Testing

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  e2e:

    runs-on: ubuntu-20.04

    steps:
      - name: Project checkout
        uses: actions/checkout@v2
      - name: Run E2E test
        uses: cypress-io/github-action@v2
